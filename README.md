# Modified-counting-sort
This is just a simple fun project, I independently developed this method (when researched I got to know its called counting sort). I have added additional flap mechanism which will limit the generation rate. hope to upgrade it
